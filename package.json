{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "remote-drop",
  "title": "Remote Drop",
  "description": "Upload a local file to one or more named remote machines over SSH and copy the remote paths.",
  "icon": "icon.png",
  "author": "sadiksaifi",
  "categories": [
    "Developer Tools",
    "Productivity"
  ],
  "license": "MIT",
  "packageManager": "pnpm@10.33.2",
  "platforms": [
    "macOS"
  ],
  "commands": [
    {
      "name": "upload-file",
      "title": "Upload File to Remote",
      "description": "Pick a local file and upload it to one or more configured remote machines over SSH.",
      "mode": "view"
    },
    {
      "name": "manage-remotes",
      "title": "Manage Remotes",
      "description": "Add, edit, duplicate, or remove named remote machine profiles.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.94.0",
    "@raycast/utils": "^1.19.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.12",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^4.1.11"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "ray lint",
    "test": "vitest run",
    "fix-lint": "ray lint --fix",
    "publish": "pnpm dlx @raycast/api@latest publish"
  }
}
