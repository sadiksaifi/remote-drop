{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "remote-drop",
  "title": "Remote Drop",
  "description": "Upload a local file to a preconfigured remote machine over SSH (scp or rsync) and copy the remote path to the clipboard.",
  "icon": "icon.png",
  "author": "sadiksaifi",
  "categories": ["Developer Tools", "Productivity"],
  "license": "MIT",
  "platforms": ["macOS"],
  "commands": [
    {
      "name": "upload-file",
      "title": "Upload File to Remote",
      "description": "Pick a local file and upload it to the configured remote host over SSH.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "remote",
      "type": "textfield",
      "required": true,
      "title": "Remote",
      "placeholder": "user@192.168.0.10",
      "description": "SSH target in the form <user>@<host>."
    },
    {
      "name": "remotePath",
      "type": "textfield",
      "required": true,
      "title": "Remote Upload Path",
      "placeholder": "/tmp/ai-uploads",
      "description": "Absolute path on the remote machine where files will be uploaded."
    },
    {
      "name": "protocol",
      "type": "dropdown",
      "required": true,
      "title": "Upload Protocol",
      "description": "Transfer tool used to upload the file.",
      "default": "rsync",
      "data": [
        { "title": "rsync", "value": "rsync" },
        { "title": "scp", "value": "scp" }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.94.0",
    "@raycast/utils": "^1.19.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.12",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "publish": "pnpm dlx @raycast/api@latest publish"
  }
}
